# Log Monitoring System

A Dockerized log monitoring system built using Django and PostgreSQL.  
This system ingests logs, stores them in PostgreSQL, and supports filtering, anomaly detection, and alerting.

---

## 🚀 Tech Stack
- Python 3.11
- Django 4.2
- Django REST Framework
- PostgreSQL 15
- Docker & Docker Compose
- Gunicorn (Production WSGI server)

---

## 🚀 Quick Start (One Command)

### Prerequisites
Install:
- Docker
- Docker Compose

### Run the project

```bash
git clone https://github.com/Dhanush65273/log_monitor.git
cd log_monitor
docker compose up
